name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  quality-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run static audit (Ruff + Mypy)
        env:
          LLM_API_KEY: ci-dummy-key
          LLM_API_URL: https://example.invalid/v1/chat/completions
        run: make lint

      - name: Run logical test suite (Pytest)
        env:
          LLM_API_KEY: ci-dummy-key
          LLM_API_URL: https://example.invalid/v1/chat/completions
        run: make test
